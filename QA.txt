二、热身阶段（即日-4.19前）
Q1:请问这个热身阶段的时间段是什么？初赛可以提交的代码包次数有限制吗？此外每个阶段提交允许提交的频次是怎么样的，任务流回放工具有没有更详细地说明？
A：没有限制，以最新版本的提交为准。任务流回放工具会在4.13前放出来。

Q2:请问这个 ttft 指的是从发出 /ask 请求到进行 /submit 提交之间的软时限是吗
[图片]
A：从决出该任务的bid胜出方开始计时，到收到最终的response为止结束。

Q3:请问 query 之后是要在下一次 query 之前决定是否接这一任务吗？
[图片]
A：不需要，以ask为准。

Q4:这个初赛会后面有类似的提交平台实时显示rank嘛，还是说最后黑盒的交上去跑一下呢？
A：黑盒提交，初赛不会有实时排行榜。

Q5:选手可以在自己学校的集群上做调试 调好了再提交评测任务吗？
A：可以。

Q6:请问这里选手服务有接口规范吗？这个“最小可运行示例”又是什么，是由 run.sh 生成的后台进程吗？
[图片]
[图片]
A：最小可运行实例指选手需要搭建的bid-ask系统，与选手自己的推理服务配合来选择性地接取任务。选手的推理服务主要和自己的bid-ask系统交互，没有严格的规范约束，但是仍然推荐使用 vLLM/OpenAI completion 风格。

Q7:请问your_secret_token在哪里获取？
A：后续由主办方统一提供。

Q8:赛题介绍文件里边有提及平台，这个平台具体是指？看完介绍，没太明白就是具体任务是？
A：平台就是生成推理请求和评估生成结果的评测系统。具体任务就是选择性地接取平台放出的推理任务并在限定延迟内完成。

Q9:请问如果我选择放弃一个任务，是可以直接不调用 /ask，然后再调用一次 /query 拿新的任务吗
A：对的，重复query会拿到新的任务。

Q10:请问在热身和预赛中，在没有其它队伍的竞争压力的情况下，是不是可以通过串行执行所有任务来获取所有任务的分数
A：可以。

Q11:这个secret token是每个队伍单独的吗？在哪里能看自己的secret token啊？
A：是单独的。后续会通过邮件发放。

Q12:没有太理解这里说的，提供任务流回放工具（4.13前）和所谓的平台是一回事吗？
A：可以理解为任务流回放工具是一个不提供具体题目和打分的平台。

Q13:请问热身阶段会提供 gpu 信息吗？那 gpu 是自己可以随便租的是吗？那 api 请求是在我本地向您服务器发发送的是吗？比如，租个比较高端的 gpu 然后提供吞吐量什么的？希望提前通知在初赛测试时用的 gpu 的信息
A：初赛提交的代码会运行在GPU服务器上，所以选手提交的代码需要能够自己使用GPU资源来拉起推理服务。GPU服务器的具体信息也会在本周内放出。

Q14:所以现在是环境是需要自己先fake一个自己测吗？
A：对。

Q15:平台大概什么时候搭建好呀？
A：平台已经搭建好了，但是初赛是线下评测，选手不会直接接触到平台，主要通过任务流回放工具来配合调试。

Q16:推理是通过api？使用api推理可以吗； 但我看开发文档挂了模型参数路径 意思是要本地下载部署吗
A：选手需要自己来部署和调整本地推理服务，不可以使用api推理。

Q17:请问会提供一个demo代码，来确保选手的环境可以正常运行嘛
A：选手可以使用任务流回放工具来验证代码的正确性。

Q18:请问模型架构有进一步的信息吗？正赛的模型架构还是这个 Qwen3-32B 吗？
[图片]
A：正赛会使用Qwen3-32B作为部署模型。

Q19:请问如果在极短时间内发出3次/query请求，我们是会拿到3个完全相同的任务，还是不同的。换句话说任务的发放策略是全局放出一道题，ask之后再放出下一道，还是同时存在不同SLA任务
A：任务的发放策略是周期性放出大量题目，所以会同时存在不同的任务。同一个任务对每个队伍的query来说只会出现一次。

Q20:请问 target_reward 对应于最终分数计算式子中的哪一项？请问这里面的 max_winners 是什么意思？
[图片]
A：即能够得到的最大分值，也就是最终分数计算式子的R_i。max_winners指的是同一个题目最多被分配给几个队伍。

Q21:请问这里的 w_task 有没有一个确定的计算公式？如果没有，选手是否能知道每个任务的 w_task 的具体值？请问这里的 w_sla 和 SLA 等级之间是否有一个对照表？请问 w_sp 和采样参数之间是否有一个对照表？
[图片]
A：选手在query题目时会得到具体的任务分值（Q20）和SLA要求、采样参数信息，没有确定的对照表。

Q22:请问出现连续错误的惩罚机制具体是什么样的？它对选手获取新任务会造成怎样的定量影响？
A：连续错误会降低信誉分，进而影响到接取任务的成功率。

Q23:如果这样的话，那初赛是怎么体现区分度的？是任务流长度无限，还是说处理任务有一个总时限？
[图片]
A：有一个总时限。

Q24:在初赛开始之前其实没有可以用作熟悉测试的地方对不对，就是说初赛前能参考的只有两个文档对嘛？然后初赛24h内完成所有代码和提交
A：还会有一个任务流回放工具。

Q25: 热身赛会有排行榜吗（已回复：目前还没有上线 弄好会第一时间同步大家）每次调试都要发邮件实时性不是很差
A：初赛的排行榜是一次性的（非实时榜单），代码以最后一次邮件提交的结果为准。

Q26:请问查询任务响应体中的max_winners参数是什么呢
A：max_winners指的是同一个题目最多被分配给几个队伍。

Q27:请问允许进行量化吗
[图片]
A：允许，但可能会影响到模型回答的正确性。

Q28:热身赛时会不会提供测试数据来调试
A：选手可以使用任务流回放工具来验证代码的正确性。

Q29:请问一个task是会包含多条推理请求吗
A：对。

Q30:请问warmup阶段的任务流回放工具什么时候开放？这个工具具体能给出哪些信息？初赛阶段的“离线开发”具体是什么含义
A：任务流回放工具会在4.13前放出来，主要提供平台侧的一些交互形式，不会给出具体的题目信息。离线开发指初赛期间不会提供在线评测和调试环境给选手，最终代码通过邮件提交。

Q31:测试的时候用的模型是 Qwen 3 32b，是说在初赛，贵方在测试我们代码的时候，如果我要求贵方用32b的量化模型的话，贵方会根据我们的要求去测？真正测试的时候应该只用 32b full precision的吧？如果可以的话，能不能提供下载模型的链接
A：全精度模型的下载链接：https://huggingface.co/Qwen/Qwen3-32B 如果在初赛中要求使用量化模型的话，可以在邮件中提供所需版本的下载模型链接。

Q32:提供 baseline 推理服务框架及示例代码这个在哪里
A：详见开发文档。推理引擎本身的代码不做限制。

Q33:请问能否自己带模型做投机解码？或者用小规格的开源qwen3
A：可以。

Q34:请问任务流回放工具和选手的secret token大概什么时候放出？
A：任务流回放工具会在4.13前放出。secret token会在初赛开始前放出。

Q35:请问初赛阶段不同赛题的发布时间是随机的吗还是会在某一时间段聚集，具体的发布流程会进行详细说明吗
A：如果赛题指的是推理任务的话，初赛的评测平台会周期性地放出任务，每次批量放出一定的题目。

Q36:请问文档中的 Qwen3-32B 就是 huggingface 上 exactly 的 Qwen/Qwen3-32B 吗？关于 setup.sh，这个脚本的权限是什么呢，是在一个具有 root 权限的容器/机器上运行的吗
A：是的，详见Q31。脚本会在一个具有 root 权限的容器上运行。

Q37:关于 run.sh 的细节，这个脚本是需要在后台启动推理服务以及调度服务，然后自身在 60s 内退出吗？但是如果允许直接生成后台进程，那这个 60s 限制的意义是什么呢
A：60s指的是拉起推理服务的时间，只对这一点做限制。

Q38:请问本地测试环境搭建是需要自建Qwen3-32B 节点吗，有没有模拟题可以导入测试
A：初赛评测时会给启动脚本提供模型路径，选手自行调试代码时需要自建节点，初赛不提供调试环境。任务流回放工具只会提供对应消息格式的交互服务，不会提供模拟题。

Q39:感觉这个标准很笼统，可能会导致各种捷径，比如使用类似llama.cpp的gguf的话，可以不用调gpu了，但是这可能不太符合比赛的初衷。请问贵方能不能指定能使用的模型？（包括量化）或者干脆只允许用全精度的模型 hhh
[图片]
A：过于激进的量化策略会导致正确率偏离模型原始表现，降低正确率进而影响最终得分。

Q40:secret token会在初赛开始前放出，那热身赛怎么提交代码
A：评审代码只有初赛和决赛两轮，热身赛期间选手自行调试和开发代码。

Q41:请问环境内会安装cuda-toolkit吗
A：会提供。

Q42:请问如果需要投机解码的话，其他模型是需要自己在setup.sh中实现拉取吗
A：是的。

Q43:到时候环境是用什么样的方式提供，docker还是真实的物理机，是docker的话能够提供一个供测试的镜像吗
A：docker。docker镜像可以参考Nvidia的Pytorch系列镜像，比如nvcr.io/nvidia/pytorch:25.11-py3

Q44:请问，初赛代码是否需要在18日某个截止时间前提交然后19日开始进行的是代码运行，代码无法修改；还是说初赛代码从19日开始可以持续开发到某个截止时间再提交去运行？
A：19日开始进行的是代码运行，代码无法修改。

Q45:请问我们应该怎么提交，平台什么时候会提供
A：初赛通过邮件的形式提交代码，任务流回放工具会在4.13前放出。

Q46:我看Q23提及程序运行存在总时间限制，这个时间限制可以公开的吗？

A：选手可以预期总运行时间为若干个小时。

Q47:我看任务队列会周期提供大量任务，这是否意味着可能出现想要访问任务队列，但是任务队列为空的情况出现。还是说任务的数据量大到不会出现无任务可做的情况。
A：不保证不出现任务队列为空的情况。

Q48:Q22提及连续错误存在惩罚机制，即降低信誉分，导致接取任务成功率下降。如果接取任务出现不成功情况，这个任务是会丢失吗？还是意味着不丢失，只是query多了一个随机失败的概率，可以通过多次query来弥补掉这个成功率下降的问题？以及是否存在信誉分的恢复机制。
[图片]
A：接取任务不成功指的是在多个选手进行bid竞争的时候，低信誉的选手更容易失败。换句话说，接取任务失败意味着这个任务已经被其他选手接取，后续query也不会再遇到这个任务。信誉分降低惩罚会在一段时间后恢复。

Q49:以及Q22提及的错误问题，什么类型的问题回答会被判定为错误，即我想了解一下这里的错误是通过正确率低于某个阈值来判定的？还是说只有正确率为0或者某些异常提交才会判定为错误？
A：具体判断标准参考开发文档的5.2 正确性评判章节，我们会以全精度模型的答题能力作为参考标准。

Q50:也就是说热身赛是可以通过邮件提交代码，然后结合返回的结果根据任务流回放工具进行调试的吗
[图片]
A：热身赛期间直接根据任务流回放工具进行调试即可，预赛才需要邮件提交代码。

Q51:比赛时候使用的数据，标准答案是通过调用Qwen3-32b获得的吗，还是真实世界的标准答案
A：标准答案是通过调用Qwen3-32b获得的且保证正确的。

Q52:提交的代码文件中是否需要包含Qwen3-32b模型，还是说在统一测试环境中已经下载Qwen3-32b模型了？
A：代码文件不需要包含模型，测试环境里已经下载了模型文件，会提供访问路径。

Q53:secret token在哪天发放，比赛当日还是提前一两天
A：会在比赛前一到两天发放。

Q54:初赛有没有竞争机制？赛制介绍中说的是只有决赛有竞争机制
[图片]
A：初赛没有竞争机制，因此只要接取任务就一定会成功。

Q55:对于某个任务，其从被平台生成并进入任务池（即可被 /query 返回）开始，到选手通过 /ask 成功获取之间，是否存在一个明确的时间有效窗口（time-to-live 或可领取时限）？
A：存在，任务在无人领取的状态下会在300s后超时，超时以后不可再被领取。

Q56:集中提问如下：
1. eval_timeout_s 是否始终固定为 600 秒？如果会变化，是否应始终以 /query 返回值为准？ A：以 /query 返回值为准。
2. SLA 截止和 eval_timeout_s 的计时起点分别是什么时刻（任务产生、/query 返回、还是 /ask 成功）？A：参考Q2。
3. 比赛总时长结束后，已 /ask 但未 /submit 的任务如何结算：还能在各自 eval_timeout_s 内提交，还是到点即停止计分？A：到点停止计分。
4. 初赛是否允许同时持有多个“已 /ask 未 /submit”任务？若允许，上限是多少？A：允许，实际上限为64个（可能会根据赛题数据有所调整，但是必定会大于测试环境下推理引擎的并发上限。）
5. /query 每次返回的是“当前唯一待决策任务”，还是“任务池中一个可选任务”？ A：任务池中一个可选任务。
6. 若某次 /query 返回任务后不 /ask，该任务是否会永久跳过，后续是否可能再次返回给同一队伍？A：见Q19。
7. /query 是否有频率限制、并发连接限制或限流规则（例如 429）？是否有推荐轮询间隔？A：实际限额为32个/s（后续可能会根据赛题数据有所调整）
8. target_reward 是否等于该任务在 C_i=1 时的满分收益？A：是。
9. 未完成惩罚与 target_reward 的关系是否可明确（是否可近似理解为某个固定倍数）？A：是。
10. loglikelihood 的 accuracy 期望提交“总 logprob”还是“平均/归一化值”？A：总 logprob。
11. loglikelihood_rolling 的 accuracy 期望提交“总 logprob”还是“平均/归一化值”？A：总 logprob。
12. generate_until 命中 until 时，提交的 response 是否应去掉停止符本身？A：是。
13. 同一任务重复 /submit 时，平台按哪次结果计分（忽略、覆盖还是报错）？A：第一次submit结果有效。

Q57:选手在比赛当日可以多次提交取最优结果吗，还是只能提交一次？
A：在初赛当日前可以多次提交，初赛线下评测时会以最后一次提交的代码为准。

Q58:请问在setup.sh运行过程中会传递什么环境变量吗？是否能在setup时读取模型做一些build cuda graph之类的工作？
A：setup.sh由选手提供，可以传递环境变量。（在启动推理引擎时）可以build cuda graph。

Q59:是否有方式让选手验证自己的代码是否能够在评测环境正常运行？
A: 参考Q43。

 Q60:请问决赛的任务规模、给出频率、计算量（promot长度）的量级能够透露吗？
A：prompt长度会整体限制在4K以内。

Q61:想问问任务流回放工具什么时候能有？（不是说13号之前提供吗？现在啥情况了）比赛时所用的gpu等情况是否确定了？（这些直接决定我们该租用什么显卡来测试并发/延迟/投机解码，从而确定后续的策略）
A：任务流回放工具预期会在13号提供。比赛时使用的测试平台硬件信息见开发文档。

Q62:请问“任务基础权重，正比于推理计算量”这里的“正比”有一个更加定量的描述吗
A：query的返回结果会给出具体的reward，请选手灵活根据reward值进行决策。

Q63:根据Q29，这是意味着generate_until和loglikelihood_rolling也会有多个message吗？如果是，此时正确性分数是怎么定义的？
A：是的，多个请求的正确性分数会取平均。

Q64:请问 loglikelihood_rolling 对于窗口参数有要求吗？如果有，那么要求是什么？
A：要提交的是整段文本的总 logprob，对窗口参数没有要求。

Q65:请问 overview 字段就是 /query 响应的数据吗？
[图片]
A：对。
Q66:同一个task内部会出现不同的任务类型吗，开发文档中，从query返回的overview来看，每条task是一种类型。从submit的示例类型来看，似乎每条task又支持不同类型？
[图片]
[图片]
A：真实评测环境里不会出现不同的类型，每个task的类型是一致的。

Q67:这个有些细节需要确认一下，就是任务生成之后到query之前有个300s的生命周期？还是任务生成之后到ask之前有个300s的生命周期？如果是后者，query的时候似乎无法获取到其生命周期相关的信息，即拿到任务不知道他的生命还剩多长时间。希望这个细节信息介绍一下具体情况。
[图片]
A：后者。如果任务在ask完成之前超时，选手不会得到成功的ask返回。query的时候不会得知任务的剩余生命周期。

Q68:Q67的回复当中，任务的生命周期完全无法观测，query拿到的是一个随时可能过期，甚至可能已经过期的任务。这个无法观测的生命周期给后续的调度带来了不小的复杂度，且与实际应用场景也有所偏差，所以想问问这个后续是否会考虑增加任务生成的时间戳等方式来开放任务生命周期的观测？而且任务流回放工具也没有模拟出任务存在生命周期的特性，这个近期是否会修改？
A：query到的任务如果在ask环节已经过期会直接造成reject，而所有ask派出的任务有固定的生命周期。因此query到的任务过期这件事与bid失败造成的结果是相当的，请选手正确处理。任务的生命周期管理和正赛的评测后端耦合，因此在任务流回放工具里没有放出。

Q69:注意到任务流回放工具里面有这些定义，但是回复又不是 streaming 的，这些值的含义是什么呢？
[图片]
A：早期选题的预定义字段，实际评测的时候不会用到。

Q70:以及想问问 generate_until 任务的输出结果是用什么算法来计算正确性评分的呢，是编辑距离/汉明距离还是其它的算法？
A：涉及正赛用题，可能不唯一。

Q71:想请问 README 和源码参数不一致时，以哪个为准？另外 run.sh 启动后会立刻开始发题吗？还是会有一点 warmup 时间？
A：回放工具仅供调试参考。涉及赛题具体规则请参照赛制介绍和开发文档，或在FAQ内提问。

Q72:想确认一下，评测环境里4张5090之间是NVLink还是PCIe互联？方便的话，能否提供 nvidia-smi topo -m 的输出示例？
[图片]
RTX 5090 之间为 PCIe 互联，不支持 NVLink。具体拓扑信息不做额外提供，建议选手在设计多卡通信策略时按 PCIe 互联进行考虑。 
Q73:另外想请问评测时用于生成参考答案的Qwen3-32B是否开启了thinking mode？
A：是。

Q74:请问现在给出的任务流回放工具和正赛使用的评测工具在哪些地方上有差异，哪些地方上保证相同？
A：任务流回放工具剥离了正赛使用的打分逻辑和对后端数据集的处理、任务构造逻辑，但是在用户交互协议层面上会保持一致。

Q75:正赛时选手容器 /home 目录的硬盘配额是多少？
A: 选手容器的可用磁盘空间不少于 200GB，足以容纳模型权重、编译产物和运行时缓存。请选手合理规划磁盘使用，避免存储不必要的大文件。

Q76:setup.sh 运行环境的网速如何呢，访问 github 等网站的速率怎么样？
A：可以保证流畅下载。正赛环节会保证所有setup.sh的下载网速。如果担心下载源不稳定可以考虑预打包部分代码。

Q77:看了回放工具的源代码，运行 setup.sh 的时候 MODEL_PATH 这些环境变量是没有传递的，那 Q58 的回答是什么意思？
A：setup.sh不会参与计时，主要用于选手安装环境。Q58的意思是选手可以在setup.sh里自行传递环境变量进行环境安装，但是CUDA graph构建这类运行态行为应该放在run.sh里执行。

Q78:能有样例数据集和热身赛榜单吗/kel
A：目前没有计划。

Q79:初赛日提交了一次可以看到结果和继续优化吗
A：初赛日是离线评测，我们会统一发布结果。

Q80:请问方便透露这些信息吗：初赛阶段任务的大致个数（或者数量级）、总时间、一个任务 messages 的最多个数
A：初赛总运行时间为若干个小时（参考Q46），任务会周期性批量放出，总量充足，足以拉开选手之间的区分度。
单个任务的 messages 数量与任务类型相关，loglikelihood 类任务（多选题）的 messages 数量取决于候选项数目, generate_until 和 loglikelihood_rolling 类任务通常包含少量 messages。具体数值不做提前公布，请选手设计系统时考虑一定的弹性。

Q81:初赛日提交一次代码后后，选手可以看到代码运行的状况，如成绩得分这些，从而有针对的再进行下一次优化进而提交第二次第三次吗？在最终提交截止前，平台是否会提供一定的过程反馈，例如运行状态、阶段性成绩或其他评测信息？
A：初赛为离线评测模式，选手在初赛日前通过邮件提交代码以最后一次为准，有且仅有一次运行代码的机会(即初赛的离线评测)初赛日统一运行评测。评测过程中不会向选手提供实时运行状态或阶段性成绩反馈，最终结果会在评测完成后统一公布。建议选手在提交前充分利用任务流回放工具（ubiservice）进行本地调试和验证。

Q82:从 ubiservice 源码看到的是，启动 run.sh 之后的 args.duration 秒内跑完所有评测过程。似乎没有看到这个 60s 限制？正赛的评测流程也是这样吗，还有说有不同
[图片]
A：ubiservice 是供选手本地调试用的任务流回放工具，其 duration 参数是评测总时长，可自行配置, 但不代表正赛中没有这个限制。回放工具主要用于协议交互层面的调试，与正赛评测流程在计时等细节上可能存在差异。

Q83:是否存在在最理想情况下也没有办法在sla内完成的任务？
A：评测任务不保证一定可以在SLA内完成，需要选手合理权衡。但需要注意，高 SLA 等级（如 Supreme、Glorious）对系统延迟的要求非常严格，选手需要在推理优化和调度策略上做充分的工作才能满足。选手可以通过 /query 查看任务概要后自主决定是否接取（参考 Q5），合理取舍是调度策略的一部分。

Q84:对提交的代码包大小有没有限制
A：代码包中不需要包含模型权重。如需额外的模型文件（如投机解码用的小模型），可以在 setup.sh 中通过网络下载（参考 Q42），也可以打包在代码包中。如果代码包确实较大，请提前联系主办方确认。

Q85:想问一下任务的SLA，时间限制就是ttft吗？比如Gold 任务都是限制6.0 s 内完成。还是说每个具体的任务都会给出具体的时间限制？我看到JSON配置文件给出的是"ttft_avg"，我猜测这里是“平均每条任务完成需要的时间”而不是“限制时间”？
A：SLA 的时间限制以配置文件中各等级的 ttft_avg 值为准，例如 Gold 等级对应 6.0 秒。这是一个任务级别的整体时间限制——即使一个任务包含多条 message（如多选题的多个候选项），选手也需要在该时间窗口内完成所有 message 的推理并一次性提交。每个 SLA 等级的时间限制是固定的，不会按单个任务再做额外调整。

比如一个task内有3条message, SLA为Gold, 那么必须在ask后的6.0秒内submit 一整个task(包含3条message的推理结果)
Q86:Q73说开了thinking mode的意思是，选手需要对任务提供的prompt做apply chat template吗？是只针对generate_until的任务吗，还是loglikelihood也需要对prompt做apply chat template？同问。Qwen3的thinking mode不是由prompt控制的吗
A: 平台使用的 Qwen3-32B 模型本身开启了 thinking mode (没有显式关闭)，参考答案也是在此模式下通过 /v1/completions 接口生成的。关于 chat template，模型路径中已包含对应的模板文件，如果需要使用, 选手可以直接使用，无需额外处理。
Q86+：Regarding Q86，还是有点没理解“模型本身开启了 thinking mode，参考答案也是在此模式下通过 /v1/completions 接口生成的”，可不可以认为选手拿到的prompt字段是带有thinking mode的template的，只需要直接用/v1/completions 风格接口生成即可？因为如果需要选手处理thinking mode这一部分的话，拿到的消息应该是messages（[{"role": "user", "content": "..."}]）格式？
A：prompt字段是原始文本。


Q87:until就是一个string吗，还是有可能有多个string，任意击中一个就停止？
[图片]
A: until 是一个字符串列表（list of strings），可以包含一个或多个停止符。生成过程中命中列表中的任意一个停止符即应停止生成。具体格式可参考开发文档中的示例：`"until": ["\n\n"]`。

Q88：参考Q58，启动推理引擎加载模型捕获cuda graph等操作可以放入setup.sh吗
A：启动推理引擎的行为应该放到run.sh中。

Q89：请问一下generate_until任务的max_gen_toks范围是多大呢
A：不超过4000。

Q90：请问一下，对于loglikelihood_rolling任务，“困惑度是否在参考值合理范围内”这里的合理值的区间是多少
A：不完全是二值判断，更接近参考值的回答会得到更高分数。

Q91:TEAM_TOKEN和secret_token有什么区别？run.sh里面通过环境变量传的TEAM_NAME=队伍名称和TEAM_TOKEN=队伍 Token是不是就不需要默认值了？然后是否需要自己创建虚拟环境还是直接用python环境？
A：相同，环境变量传递的值与分发给各个队伍的token值一致。环境方面创建虚拟环境和直接使用python环境均可。

Q92:我们在用本地 ubiservice 模拟器压测时发现，/ask 返回的题目 JSON 中 eval_timeout_s 字段偶尔会出现缺失或为 null 的情况，导致代码在做动态超时转换时报 NoneType 错误。想确认一下：正式比赛时，官方下发的题目数据是否保证 eval_timeout_s 为必填且非空字段？ 还是说我们也需要针对该字段缺失做兜底逻辑？
A：ubiservice 是供选手本地调试用的任务流回放工具，其任务构造做了简化，eval_timeout_s 等评测元信息字段（包括 eval_task_name、eval_request_type、eval_sampling_param）均未填充，这是预期行为。正式比赛环境中这些字段会正常提供。建议选手在代码中对该字段做合理的兜底处理（如设置默认超时值），以确保本地调试和正式评测均能正常运行。

Q93:分值最高的那批问题里，大概有什么来自 math 这个 benchmark 的例子？ 
A: 评测范围请参考开发文档中的说明：涵盖数学推理、常识问答、语言理解、知识推理、语言建模等多个领域。举几个例子: SVAMP、SciQ、CommonsenseQA

Q94:排名是怎么排的，分数和耗时,优先提交？
A：排名以总分（Score）为唯一依据。总分计算公式为：Score = 成功任务收益之和 - 未完成惩罚之和 - 恶意提交惩罚之和 - 崩溃惩罚之和（参考赛制介绍 2.5 节）。不考虑耗时或提交顺序等因素，分数相同的情况下排名并列。

Q95:Regarding Q86 “模型本身开启了 thinking mode，参考答案也是在此模式下通过 /v1/completions 接口生成的。关于 chat template，模型路径中已包含对应的模板文件，选手可以直接使用，无需额外处理。” 是否可以认为是 选手拿到的 prompt 字段依然是原始的裸文本，需要自己在代码里对generate和likelihood任务调用 apply_chat_template 来组装字符串，然后再发给 /v1/completions， 而 likelihood_rolling任务无需apply_chat_template？但是最小运行示例里并没有提及，所以也可能可以推测是选手拿到的prompt已经 apply_chat_template 过了？
A：选手拿到的 prompt 是原始文本, 没有apply chat template. 拿到原始文本后, 是否apply template需要自行权衡。

Q95+：自行权衡？意思是还得猜标准答案生成的时候有没有 apply chat template 吗？
[图片]
A：赛题考核的是得到正确答案而非和reference results严格相等的答案，因此是否使用chat模式在design space当中。reference results的生成基于原始文本。
Q96:想确认一下 run.sh 的流程。我理解的是，run.sh 启动后，应该用后台进程的方式启动推理服务和选手客户端，然后脚本本身退出，保留后台进程仍然在运行，从启动到脚本退出的时间不超过 60s。run.sh 退出之后评测机就会开始放出任务。是这样吗？setup.sh是不是负责装一下依赖，万一网络不好依赖没进去不完了
A：setup.sh不会计入到60s限时当中。线下评测会排除网络因素的影响。60s的限制主要是要求选手尽快拉起推理服务，run.sh是否用后台进程的方式并不影响。

Q97:注意到generate_until的评测集通常会用\n做停止符，那模型在think阶段就遇到\n截断算是正常的吗，官方结果中会出现这样的情况吗
A：不会出现这种情况。遇到\n截断不是正常情况，请选手注意规避。在实测数据集上\n也不是常见停止符。

Q98:关于正式比赛环境中字段的问题，/query或/ask中得到的采样参数是contest.json中提供的几种类别吗（比如开发文档中的Deterministic）以及对于generate_until的任务，本地测试工具提供的msg中eval_gen_kwargs给出了全部的采样参数，而开发文档中的样例中没有全部给出，这个正式测试是怎样的
A：overview中的采样参数是contest.json中提供的几种类别，但是所有请求的msg中eval_gen_kwargs会给出全部的采样参数。具体可以参考开发文档4.5 Message 字段说明（generate_until）。

Q99:“赛题考核的是得到正确答案而非和reference results严格相等的答案”，那在正式答案前的<think>标签中的内容会影响最终得分吗，评测时是否会自动去除
A：标签本身不会直接影响最终得分，打分程序有提取逻辑。

Q100:请问一下，之前有提到，部分任务不保证sla内能完成，如果我不接取这部分任务，这些任务会积压在服务器里，而且这部分题目是不会过期的，达到上限（1100条左右）会raise HTTPException(400, "Too many tasks")，回放工具中具体逻辑是matcher.py的155行，正式比赛中是否有类似逻辑，1100条在几个小时比赛中不是一个很大的数，很容有达到上限，正式比赛中是否有这部分不被接取题目的过期逻辑
A：在正式比赛里无人接取的任务会自动老化超时被释放掉，不会影响到后续的任务查询。详见Q55。


Q101:这个回答的意思是，generate_until 任务的评分是从语义层面进行评分，而不是从输出序列的相似性层面评分吗
[图片]
A：对。


Q102:比如 “The answer is 42” 和 “42” 的评分是一样的吗
A: 对。

Q103:请问一下 开发文档 JSON 配置文件是不是真实的？还是只是一个example
[图片]
A: 格式是真实的，具体数值可能会有所调整。


Q104:想问一下：因为 Qwen3 模型自身的特性，如果不使用 apply chat template，直接进行纯文本的补全的话，通常补全结果中会混杂很多思维链内容。而 Q95+ 里面提到 reference result 的生成基于纯文本。那么评测数据是保证采用了合适的 prompt 使得生成结果是合理的具有参考价值的吗（相对于混乱思维链而言）？
A：是的。


Q105:请问这个 4K 是 4000 还是 4096
[图片]
A：4096。

Q106:请问query三类任务数量是均匀分布的吗
A：不做保证。

Q107:提交规范中有CONFIG_PATH的环境变量，对于client来说只在examples/fake_submission/client.py 这里用到，用于读取PLATFORM_URL，除了这个内容，还有其他作用吗
A：里面的所有信息都可以用于决策，但是没有额外的硬性使用要求。

Q108:Qwen3-32B 默认开启 thinking 模式，参考答案通过 /v1/completions 补全接口生成，官方标准输出为 “输入末尾续写 + ... 显式标签”，是否意味着参考答案会是下面这种带完整标签、纯续写、重复问题、结构混乱、非标准问答的格式？
典型标准输出

输入问题：
1+1 等于几

/v1/completions 补全输出：
1+1 等于几。这是基础算术题，根据加法规则，1 加 1 的结果是 2，需要确认运算无误后给出最终答案。所以 1+1 的答案是 2。
[图片]
A：在这个case里只要回答出2就能够得分，判分逻辑只要求能够从回答里提取出正确的数字。
Q109:请问困惑度是否可以提供具体的定义方式，以及对于generate模式的score计算是否可以提供更加详细一点的说明呢
A：关于困惑度的具体定义可以参考语言模型的困惑度指标定义perplexity（PPL）。对于generate模式，可以强调一下，对于回答的score首先会有一个绝对的对错（比如数值计算是否正确），判分逻辑基于回答的内容得到一个绝对分值，然后再用我们的reference_score （参考答案得分）来修正。

Q110:请问从题库中提供的答案是真正用来参考的答案吗?还请问连续多次答错题目的额外惩罚有公式吗，或者连续多次提交错误的具体定义是多少次
A：判分逻辑使用的答案一定是正确答案。恶意提交的具体触发次数不予公布，可以默认是一个较大的数。

Q111:请问赛制文档提到2.5 总分，Score = 成功任务收益之和 - 未完成惩罚之和 - 恶意提交惩罚之和 - 崩溃惩罚之和 但是Q22回答又说“连续错误会降低信誉分，进而影响到接取任务的成功率。”而没提到存在“ - 恶意提交惩罚之和”，那么连续错误到底会不会影响score？还是说只影响信誉分呢
[图片]
A：会影响score。

Q112:两个问题，setup.sh允许运行的总时长，以及下载包的网速大概是多少
A：20分钟；百兆级。

Q113:请问如果有投机解码的话，小模型的提交方式是什么？放在提交的主文件里还是必须要在setup中下载？
A：都可以。

Q114:思维链部分是否需要截断？如果有保留思维链部分是否还属于“正确答案”？以及回答是否有字数上限？这里如果遇到了until/max_gen_toks，但是模型产生的答案不正确/完整该怎么判？以及如果需要保留思维链，但是思维链已经超过了max_gen_toks怎么办？
[图片]
A：以数学题为例，如果在给定的输出长度内没有输出正确答案（目标数字），会被判错。

Q115: